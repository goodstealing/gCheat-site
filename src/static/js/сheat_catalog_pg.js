
var myCarousel = new bootstrap.Carousel(document.getElementById('myCarousel'), {
  interval: 5000, // Интервал между переключениями в миллисекундах (необязательно)
  wrap: true // Зацикливание карусели
});
